# Infraestructura, Red y Proxy — NO van al repositorio público
PROXY/
nginx.conf
docker-compose.yml
Dockerfile

# Reportes, Handoffs y Bitácoras de Recuperación
REPORTES/
HANDOFF.md
GUIA_RECUPERACION_TOTAL.md

# Plantillas de entorno
.env.example
# Datasets físicos con información fiscal (RUCs)
data/raw/